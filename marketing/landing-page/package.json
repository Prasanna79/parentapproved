{
  "name": "parentapproved-landing-page",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler pages deploy . --project-name parentapproved-tv"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "wrangler": "^4.0.0"
  }
}
